{
  "name": "firestore-tests",
  "version": "1.0.0",
  "description": "Firestore Security Rules Tests",
  "main": "index.js",
  "scripts": {
    "test": "firebase emulators:exec --project chillgo-61439 --config ../firebase.test.json --only firestore,storage \"node ./node_modules/mocha/bin/mocha.js rules.test.js --reporter spec\"",
    "migrate:dry-run": "node migrate_schema.js",
    "migrate:apply": "node migrate_schema.js --apply"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.2",
    "firebase-admin": "^12.0.0",
    "mocha": "^10.2.0"
  }
}
