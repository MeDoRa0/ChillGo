{
  "name": "firestore-tests",
  "version": "1.0.0",
  "description": "Firestore Security Rules Tests",
  "main": "index.js",
  "scripts": {
    "test": "firebase emulators:exec --project chillgo-61439 --config ../firebase.json --only firestore,storage \"mocha rules.test.js\""
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.2",
    "firebase-admin": "^12.0.0",
    "mocha": "^10.2.0"
  }
}
