import {strict as assert} from "assert";import {Timestamp} from "firebase-admin/firestore";import {tally} from "../../src/agreement/agreement_tally";
describe("agreement tally",()=>{it("filters ineligible voters and expired times and derives ties",()=>{const now=new Date("2030-01-01T00:00:00Z");const proposals=[{id:"future-a",category:"time" as const,timeValue:Timestamp.fromDate(new Date("2030-01-02"))},{id:"future-b",category:"time" as const,timeValue:Timestamp.fromDate(new Date("2030-01-03"))},{id:"expired",category:"time" as const,timeValue:Timestamp.fromDate(new Date("2029-01-01"))}];const result=tally("time",proposals,[{userId:"a",category:"time",proposalId:"future-a"},{userId:"b",category:"time",proposalId:"future-b"},{userId:"removed",category:"time",proposalId:"future-a"},{userId:"a",category:"time",proposalId:"expired"}],new Set(["a","b"]),now);assert.deepEqual(result.leaders.sort(),["future-a","future-b"]);assert.equal(result.participatingVoters,2);});
it("rejects zero-vote categories by returning no leaders",()=>assert.deepEqual(tally("location",[],[],new Set(),new Date()).leaders,[]));});
