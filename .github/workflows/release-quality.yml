name: Release quality

on:
  pull_request:
  push:
    branches: [codex/008-production-readiness]

jobs:
  automated-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.8
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            functions/package-lock.json
            firestore_tests/package-lock.json
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - run: flutter pub get
      - run: cp lib/firebase_options.example.dart lib/firebase_options.dart
      - run: flutter analyze
      - run: flutter test
      - run: npm ci && npm test
        working-directory: functions
      - run: npm ci
        working-directory: firestore_tests
      - run: npm test
        working-directory: firestore_tests
  android-package:
    runs-on: ubuntu-latest
    needs: automated-gates
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.8
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - run: flutter pub get
      - run: cp lib/firebase_options.example.dart lib/firebase_options.dart
      - run: cp android/app/google-services.example.json android/app/google-services.json
      - run: flutter build appbundle --release
  ios-metadata:
    runs-on: macos-latest
    needs: automated-gates
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.8
      - run: flutter pub get
      - run: cp lib/firebase_options.example.dart lib/firebase_options.dart
      - run: flutter build ipa --release --no-codesign
